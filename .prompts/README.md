# Prompt Notes

Codex Web での継続開発用メモ。

- 変更前に `TASKS.md` の対象Phaseを確認。
- 実装後は `npm run build-nolog || npm run build` で検証。
- 新規依存は SECURITY_CHECKLIST に沿って理由を記録。
- 画像素材は `public/assets/generated` の SVG を優先。
